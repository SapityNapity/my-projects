#include <stdio.h>
int main(){
    int result = 0;
    for(int i = 0; i <= 50; i+=2){
       result += i;
    }
    printf("%d\n", result);
}
/*  Sum of Even Numbers: Write a program to find the sum of
    all even numbers between 1 to 50 using a for loop. */
